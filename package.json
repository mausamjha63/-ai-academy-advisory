{
  "name": "ai-academic-advisor",
  "version": "1.0.0",
  "scripts": {
    "build-local": "python manage.py collectstatic --noinput && mkdir -p public/static && cp -r staticfiles/* public/static/"
  }
}
